# Tasks

- Add timeouts for git subprocesses
- Make git fetch/pull fully non-interactive
- Lower the default worker count and allow override
- Split UI state calculation from rendering
- Add a minimal smoke test
